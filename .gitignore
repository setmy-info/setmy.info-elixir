# Mix / Hex build output
/_build
/deps
/.fetch
*.ez
*.beam

# `mix hex.build` leaves <package>-<version>.tar in the app directory
*.tar

# ExDoc (`mix docs`) and ExCoveralls (`mix coverage`) output
/doc
/cover
**/cover/

# Runtime secrets
/config/*.secret.exs

erl_crash.dump

# Editors / language servers
.elixir_ls/
.lexical/
.idea/
.lsp/
.vscode/
