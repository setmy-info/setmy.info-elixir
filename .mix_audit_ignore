# mix_audit ignore list - deliberately visible and reasoned, not a silent
# suppression: an entry documents why a finding is accepted and when it must
# be re-reviewed. One advisory id per line, `#` comments.
#
# Empty since cowboy 2.18.0 / cowlib 2.19.0 / ranch 2.2.1 (2026-08-29):
# `mix deps.audit` reports nothing against that set. The two cowlib entries
# that used to live here (EEF-CVE-2026-43969, EEF-CVE-2026-43966) were tied
# to cowlib 2.18.0 and are gone with the upgrade. Note that Hex's own
# `mix hex.audit` still lists cowlib 2.19.0 advisories from its (broader)
# database; those are accepted, with reasons, in `hex: [ignore_advisories:]`
# in mix.exs - Hex's own equivalent of this file.
